<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>BrandNewCar — Login / Sign up</title>
  <meta name="description" content="Login and sign up for BrandNewCar — demo page" />

  <style>
    :root{
      --bg1: #0f1724;
      --bg2: #071130;
      --card: #0b1220;
      --accent: #ff6b00;
      --accent-2: #ffd27a;
      --muted: #9aa4b2;
      --success: #16a34a;
      --danger: #ef4444;
      --glass: rgba(255,255,255,0.04);
      --radius: 12px;
      font-family: Inter, ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    }

    html,body{height:100%;}
    body{
      margin:0;
      min-height:100%;
      background: radial-gradient(1200px 600px at 10% 10%, rgba(255,107,0,0.06), transparent),
                  linear-gradient(180deg, var(--bg1), var(--bg2));
      color: #e6eef6;
      -webkit-font-smoothing:antialiased;
      -moz-osx-font-smoothing:grayscale;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:24px;
    }

    .container{
      width:100%;
      max-width:980px;
      display:grid;
      grid-template-columns: 1fr 420px;
      gap:28px;
      align-items:center;
    }

    /* Left marketing panel */
    .hero{
      padding:28px;
      border-radius:var(--radius);
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      box-shadow: 0 8px 30px rgba(2,6,23,0.6);
    }
    .brand {
      display:flex;
      gap:14px;
      align-items:center;
      margin-bottom:18px;
    }
    .logo {
      width:56px;height:56px;border-radius:12px;
      background: linear-gradient(135deg,var(--accent),var(--accent-2));
      display:inline-grid;place-items:center;
      font-weight:700;color:#081028;font-size:20px;
      box-shadow: 0 6px 18px rgba(255,107,0,0.18);
    }
    h1{margin:0 0 8px 0;font-size:28px;}
    p.lead{margin:0;color:var(--muted);line-height:1.5}

    .features{
      margin-top:22px;
      display:grid;
      gap:12px;
    }
    .feature{
      display:flex;gap:12px;align-items:flex-start;
      background:var(--glass);padding:12px;border-radius:10px;
      border:1px solid rgba(255,255,255,0.02);
    }
    .feature .icon{
      width:36px;height:36px;border-radius:8px;
      display:grid;place-items:center;background:linear-gradient(135deg, rgba(255,107,0,0.12), rgba(255,210,122,0.06));
      color:var(--accent-2);font-weight:600;
    }
    .feature small{color:var(--muted);}

    /* Right form card */
    .card{
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      padding:20px;border-radius:16px;
      border:1px solid rgba(255,255,255,0.03);
      box-shadow: 0 10px 30px rgba(2,6,23,0.6);
    }

    .tabs{
      display:flex;
      gap:8px;
      background:transparent;
      margin-bottom:14px;
    }
    .tab {
      flex:1;
      text-align:center;
      padding:10px 12px;
      border-radius:10px;
      cursor:pointer;
      font-weight:600;
      color:var(--muted);
      user-select:none;
    }
    .tab.active{
      background:linear-gradient(90deg, rgba(255,107,0,0.12), rgba(255,210,122,0.06));
      color: #fff;
      box-shadow: 0 6px 18px rgba(255,107,0,0.07) inset;
    }

    form{display:grid;gap:12px}
    label{font-size:13px;color:var(--muted);display:block;margin-bottom:6px}
    input[type="email"],
    input[type="password"],
    input[type="text"]{
      width:100%;
      padding:12px 12px;
      border-radius:10px;
      border:1px solid rgba(255,255,255,0.03);
      background:transparent;color:var(--accent-2);
      outline:none;font-size:15px;
    }
    .row{display:flex;gap:12px}
    .row .col{flex:1}

    .controls{
      display:flex;align-items:center;justify-content:space-between;font-size:14px;color:var(--muted);
    }
    .btn{
      padding:10px 14px;border-radius:10px;border:0;background:linear-gradient(90deg,var(--accent),#ff8b3a);
      color:#071028;font-weight:700;cursor:pointer;font-size:15px;
      width:100%;
    }
    .outline{
      background:transparent;border:1px solid rgba(255,255,255,0.06);color:var(--muted);
    }

    .socials{display:flex;gap:8px}
    .socials .btn{flex:1;padding:10px;border-radius:10px;background:rgba(255,255,255,0.02);color:var(--muted);font-weight:600}

    .muted-help{font-size:13px;color:var(--muted);text-align:center;margin-top:8px}
    .error{color:var(--danger);font-size:13px}
    .success{color:var(--success);font-size:13px}

    .small {
      font-size:13px;color:var(--muted);
    }

    /* small screens
